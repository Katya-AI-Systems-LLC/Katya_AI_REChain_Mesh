# macOS Build & Packaging

- Requires Xcode and desktop enabled: `flutter config --enable-macos-desktop`
- Build:
  - `flutter build macos --release`
- Signing/entitlements: see `macos/Runner/*.entitlements` and set team in Xcode.

# International Forges Mirroring

Supported remotes (optional):
- GitHub: `git remote add github <ssh-url>`
- GitLab: `git remote add gitlab <ssh-url>`
- Bitbucket: `git remote add bitbucket <ssh-url>`

Use `scripts/push_mirrors.sh` to push to all configured remotes.

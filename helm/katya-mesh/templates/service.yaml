apiVersion: v1
kind: Service
metadata:
  name: {{ include "katya-mesh.fullname" . }}
  labels:
    {{- include "katya-mesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "katya-mesh.selectorLabels" . | nindent 4 }}


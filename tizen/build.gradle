# Tizen Platform Configuration
# Generated by Flutter

# Installation began at: 2024-01-15 12:00:00 +0300

include ':app'

def localRepositories = new ArrayList()
for (def file : project.rootProject.allprojects) {
    localRepositories.add(file.repositories)
}

def flutterProjectRoot = rootProject.projectDir.parentFile.toPath()

def plugins = new Properties()
def pluginsFile = new File(flutterProjectRoot.toFile(), '.flutter-plugins')
if (pluginsFile.exists()) {
    pluginsFile.withReader('UTF-8') { reader -> plugins.load(reader) }
}

plugins.each { name, path ->
    def pluginDirectory = flutterProjectRoot.resolve(path).resolve('tizen').toFile()
    if (pluginDirectory.exists()) {
        include ":$name"
        project(":$name").projectDir = pluginDirectory
    }
}

project(':app').projectDir = new File(flutterProjectRoot.toFile(), 'tizen')

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://developer.samsung.com/repository/tizen' }
        maven { url 'https://developer.samsung.com/repository/tizen-emulator' }
        localRepositories.each { repository ->
            add(repository)
        }
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'com.samsung.tizen'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

android {
    namespace 'com.katyaairechainmesh.app'
    compileSdkVersion flutter.compileSdkVersion
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        applicationId 'com.katyaairechainmesh.app'
        minSdkVersion flutter.minSdkVersion
        targetSdkVersion flutter.targetSdkVersion
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
        multiDexEnabled true

        ndk {
            abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86', 'x86_64'
        }

        manifestPlaceholders = [
            'appAuthRedirectScheme': 'com.katyaairechainmesh.app',
            'TIZEN_VERSION': '7.0.0',
            'SAMSUNG_SDK_VERSION': '4.0.0',
            'TIZEN_TV_VERSION': '7.0.0',
            'TIZEN_WEARABLE_VERSION': '5.5.0',
            'TIZEN_MOBILE_VERSION': '6.5.0'
        ]
    }

    signingConfigs {
        release {
            keyAlias 'katya-rechain-mesh-key'
            keyPassword System.getenv('TIZEN_KEY_PASSWORD')
            storeFile file(System.getenv('TIZEN_KEYSTORE_PATH') ?: 'tizen/keystore.jks')
            storePassword System.getenv('TIZEN_KEYSTORE_PASSWORD')
        }

        samsung {
            storeFile file('tizen/samsung_keystore.jks')
            storePassword System.getenv('SAMSUNG_KEYSTORE_PASSWORD')
            keyAlias 'samsung-key'
            keyPassword System.getenv('SAMSUNG_KEY_PASSWORD')
        }
    }

    buildTypes {
        debug {
            applicationIdSuffix '.debug'
            debuggable true
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }

        release {
            signingConfig signingConfigs.samsung
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'

            ndk {
                abiFilters 'armeabi-v7a', 'arm64-v8a', 'x86_64'
            }
        }

        samsung {
            initWith release
            applicationIdSuffix '.samsung'
            versionNameSuffix '-samsung'
            signingConfig signingConfigs.samsung
        }

        profile {
            debuggable true
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }

    buildFeatures {
        buildConfig true
    }

    flavorDimensions 'platform', 'distribution', 'device'
    productFlavors {
        development {
            dimension 'platform'
            applicationIdSuffix '.dev'
            versionNameSuffix '-dev'
        }

        staging {
            dimension 'platform'
            applicationIdSuffix '.staging'
            versionNameSuffix '-staging'
        }

        production {
            dimension 'platform'
        }

        samsung {
            dimension 'distribution'
            manifestPlaceholders['SAMSUNG_SDK_VERSION'] = '4.0.0'
        }

        google {
            dimension 'distribution'
            manifestPlaceholders['GMS_VERSION'] = '21.0.0'
        }

        tizen_tv {
            dimension 'device'
            manifestPlaceholders['TIZEN_TV_VERSION'] = '7.0.0'
        }

        tizen_wearable {
            dimension 'device'
            manifestPlaceholders['TIZEN_WEARABLE_VERSION'] = '5.5.0'
        }

        tizen_mobile {
            dimension 'device'
            manifestPlaceholders['TIZEN_MOBILE_VERSION'] = '6.5.0'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    buildFeatures {
        buildConfig true
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation 'androidx.multidex:multidex:2.0.1'
    implementation 'androidx.security:security-crypto:1.1.0-alpha06'
    implementation 'androidx.biometric:biometric:1.1.0'

    // Samsung Tizen SDK
    implementation 'com.samsung.tizen:tizen-sdk:7.0.0'
    implementation 'com.samsung.tizen:tv-sdk:7.0.0'
    implementation 'com.samsung.tizen:wearable-sdk:5.5.0'
    implementation 'com.samsung.tizen:mobile-sdk:6.5.0'

    // Samsung Galaxy ecosystem
    implementation 'com.samsung.android:sdk:4.0.0'
    implementation 'com.samsung.android:samsung-account:4.0.0'
    implementation 'com.samsung.android:samsung-pay:4.0.0'
    implementation 'com.samsung.android:samsung-health:4.0.0'
    implementation 'com.samsung.android:samsung-cloud:4.0.0'

    // Samsung Bixby integration
    implementation 'com.samsung.android:bixby:4.0.0'
    implementation 'com.samsung.android:bixby-vision:4.0.0'

    // Samsung Knox security
    implementation 'com.samsung.android:knox:4.0.0'
    implementation 'com.samsung.android:knox-attestation:4.0.0'
    implementation 'com.samsung.android:knox-license:4.0.0'

    // Samsung SmartThings integration
    implementation 'com.samsung.android:smartthings:4.0.0'
    implementation 'com.samsung.android:smartthings-cloud:4.0.0'

    // Blockchain and crypto dependencies
    implementation 'org.web3j:core:4.9.8'
    implementation 'org.web3j:crypto:4.9.8'
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.squareup.okhttp3:logging-interceptor:4.12.0'

    // Korean market dependencies
    implementation 'com.kakao.sdk:kakao-common:2.15.0'
    implementation 'com.kakao.sdk:kakao-user:2.15.0'
    implementation 'com.kakao.sdk:kakao-pay:2.15.0'
    implementation 'com.naver.sdk:naver-login:5.8.0'
    implementation 'com.naver.sdk:naver-pay:5.8.0'

    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.mockito:mockito-core:5.8.0'
    testImplementation 'androidx.test:core:1.5.0'
    testImplementation 'org.robolectric:robolectric:4.11.1'

    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
    androidTestImplementation 'androidx.test:runner:1.5.2'
    androidTestImplementation 'androidx.test:rules:1.5.0'
}

// Samsung Tizen configuration
tizen {
    profile 'tv'
    profile 'wearable'
    profile 'mobile'

    tv {
        version '7.0'
        architecture 'armv7', 'aarch64', 'x86'
        securityProfile 'platform'

        signing {
            profile 'tv'
            keyFile file('tizen/tizen-tv.p12')
            keyPassword System.getenv('TIZEN_TV_KEY_PASSWORD')
        }
    }

    wearable {
        version '5.5'
        architecture 'armv7'
        securityProfile 'platform'

        signing {
            profile 'wearable'
            keyFile file('tizen/tizen-wearable.p12')
            keyPassword System.getenv('TIZEN_WEARABLE_KEY_PASSWORD')
        }
    }

    mobile {
        version '6.5'
        architecture 'armv7', 'aarch64'
        securityProfile 'platform'

        signing {
            profile 'mobile'
            keyFile file('tizen/tizen-mobile.p12')
            keyPassword System.getenv('TIZEN_MOBILE_KEY_PASSWORD')
        }
    }
}

name: katya_frog_backend
description: Dart Frog backend for Katya AI REChain Mesh (local sync & helpers)
version: 0.1.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dart_frog: ^1.0.0
  shelf: ^1.4.0
